# After CI publish: streamProviders selects the pre-built image tag (leave image.backend.tag empty).
eventore:
  streamProviders:
    - KAFKA

image:
  backend:
    pullPolicy: Always
  frontend:
    tag: "latest"
    pullPolicy: Always

ingress:
  host: eventore.example.com
