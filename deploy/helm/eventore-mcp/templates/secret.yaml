{{- if .Values.mcp.authToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eventore-mcp.fullname" . }}
  labels:
    {{- include "eventore-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  auth-token: {{ .Values.mcp.authToken | quote }}
{{- end }}
